name: Check Version

on:
  workflow_dispatch:
    inputs:
      message:
        description: 'Message to include in the triggered workflow (optional)'
        required: false
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  release:
    types:
      - released
  schedule:
    - cron: "0 0 * * *"

permissions:
  actions: write  # 授予 actions 权限

jobs:
  main:
    name: Check Version
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.x'
        cache: 'pip'
    - name: Install dependencies
      run: pip install -r requirements.txt
    - name: Debug
      run: |
          echo "API URL: https://api.github.com/repos/${{ github.repository }}/actions/workflows/check_version.yml/dispatches"
          echo "Authorization: token ${{ secrets.GITHUB_TOKEN_TRIGGER }}"
          echo "Request Body: {\"ref\":\"main\"}"
    - name: Trigger Workflow Dispatch
      run: |
          curl -X POST \
            -H "Accept: application/vnd.github.v3+json" \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN_TRIGGER }}" \
            -H "User-Agent: My-GitHub-Action" \
            https://api.github.com/repos/${{ github.repository }}/actions/workflows/check_version.yml/dispatches \
            -d '{"ref":"main"}'
    - name: Check Version
      run: |
        python check_version.py > README.md
    - name: Commit change
      run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git pull origin main
          git add README.md
          git commit -m "$(date '+%Y-%m-%d %H:%M:%S') 更新版本信息"
    - name: Push
      uses:  ad-m/github-push-action@master
      with:
        branch: main
